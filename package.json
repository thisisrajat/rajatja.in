{
  "name": "rajatja.in",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "classnames": "^2.2.6",
    "fontfaceobserver": "^2.0.13",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-router-dom": "^4.3.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "format": "prettier --write",
    "github-deploy": "git subtree push --prefix build origin gh-pages",
    "push": "npm run build && npm run github-deploy",
    "serve": "cd build && python -m SimpleHTTPServer 8091"
  },
  "devDependencies": {
    "prettier": "^1.13.7"
  }
}
